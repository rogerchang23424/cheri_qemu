name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  checks: write
  pull-requests: write

jobs:
  build-and-test:
    name: build-and-test (${{ matrix.host }})
    runs-on: ${{ matrix.runs-on }}
    container: ${{ matrix.container }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - host: linux
            runs-on: the-capable-hub-aws-ci-amd64
            container: capablehub/tch-ubuntu-24-builder
            targets: "arm-softmmu,aarch64-softmmu,morello-softmmu,mips64-softmmu,mips64cheri128-softmmu,riscv64-softmmu,riscv64xcheri-softmmu,riscv32-softmmu,riscv32xcheri-softmmu,x86_64-softmmu,riscv32cheristd-softmmu,riscv64cheristd-softmmu,riscv32y-softmmu,riscv64y-softmmu"
          - host: macos
            runs-on: macos-latest
            container: ''
            # The host-specific code (9pfs in particular) is compiled once for
            # all targets, so a subset is enough to cover the macOS-only paths.
            targets: "aarch64-softmmu,riscv64-softmmu,riscv64cheristd-softmmu,riscv32y-softmmu,riscv64y-softmmu,x86_64-softmmu"

    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'false'

      - name: Install dependencies
        if: matrix.host == 'linux'
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
              python3 \
              python-is-python3 \
              ninja-build \
              pkgconf \
              libglib2.0-dev \
              libcap-ng-dev \
              libattr1-dev \
              libfdt-dev \
              libslirp-dev \
              libpixman-1-dev

      - name: Install dependencies
        if: matrix.host == 'macos'
        run: |
          brew install ninja pkgconf glib dtc libslirp pixman python-setuptools meson

      - name: Configure
        run: |
          mkdir build
          cd build
          # Homebrew's prefix isn't on clang's default search path, so
          # meson can't find libfdt etc without this (see freebsd.yml).
          extra_flags=""
          if [ "${{ matrix.host }}" = "macos" ]; then
            # Homebrew python 3.14 is not compatible with the bundled meson, use system meson.
            extra_flags="--meson=meson --extra-cflags=-I$(brew --prefix)/include --extra-ldflags=-L$(brew --prefix)/lib"
          fi
          ../configure \
            --enable-werror \
            --enable-virtfs \
            --disable-docs \
            --disable-cocoa \
            --enable-fdt=system \
            --enable-slirp=system \
            --disable-capstone \
            --disable-linux-user \
            --target-list="${{ matrix.targets }}" \
            $extra_flags

      - name: Build
        run: |
          cd build
          make -j$(getconf _NPROCESSORS_ONLN)

      - name: Run Morello tests
        if: matrix.host == 'linux'
        run: |
          git clone https://github.com/rems-project/morello-generated-tests.git
          pwd
          python -m venv .venv
          . .venv/bin/activate
          pip install pytest-xdist
          cd tests/morello && pytest \
            --morello-tests-dir=$GITHUB_WORKSPACE/morello-generated-tests \
            --qemu=$GITHUB_WORKSPACE/build/qemu-system-morello \
            --junit-xml=$GITHUB_WORKSPACE/morello-test-results.xml

      - name: Run QEMU tests
        run: |
          cd build
          pwd
          _meson=../meson/meson.py
          if [ "${{ matrix.host }}" = "macos" ]; then
            _meson=meson
          fi
          ${_meson} test --print-errorlogs --timeout-multiplier=2
          cp meson-logs/testlog.junit.xml $GITHUB_WORKSPACE/qemu-test-results.xml
        continue-on-error: true

      # The regular action only works on Linux since it runs in a container;
      # macOS (and Windows) runners need the composite variant instead.
      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: matrix.host == 'linux' && !cancelled()
        with:
          files: "*-test-results.xml"
          action_fail_on_inconclusive: 'true'
          # Would require additional permissions at the repository level
          check_run: 'false'
          # QEMU test results produce large output files
          large_files: 'true'

      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action/composite@v2
        if: matrix.host == 'macos' && !cancelled()
        with:
          files: "*-test-results.xml"
          action_fail_on_inconclusive: 'true'
          check_run: 'false'
          large_files: 'true'
